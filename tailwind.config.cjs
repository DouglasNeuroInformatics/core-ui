// @ts-ignore - setting allowImportingTsExtensions to true will break at runtime in library usage, but must be used for storybook
const config = require('./src/tailwind/config.cts');

module.exports = config();
